{% extends 'admin/base.html.twig' %}

{% block title %}Fournisseur{% endblock %}

{% block body %}
<section class="supplier-show">
    <div class="container">
        <h1>Fournisseur</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ supplier.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ supplier.name }}</td>
                </tr>
                <tr>
                    <th>Address</th>
                    <td>{{ supplier.address }}</td>
                </tr>
                <tr>
                    <th>PhoneNumber</th>
                    <td>{{ supplier.phoneNumber }}</td>
                </tr>
            </tbody>
        </table>
        <a href="{{ path('app_supplier_edit', {'id': supplier.id}) }}" class="btn-register btn btn-warning">Modifier</a>
        <a href="{{ path('app_supplier_index') }}" class="btn btn-secondary">Retour à la liste</a>

        {{ include('admin/supplier/_delete_form.html.twig') }}

    </div>

</section>
    
{% endblock %}
