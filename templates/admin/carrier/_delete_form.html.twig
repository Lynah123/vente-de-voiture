<form class="mt-4" method="post" action="{{ path('app_carrier_delete', {'id': carrier.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cer élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carrier.id) }}">
    <button class="btn btn-danger px-4">Supprimer</button>
</form>
