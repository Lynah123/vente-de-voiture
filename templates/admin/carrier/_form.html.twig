{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary px-4 btn-register">{{ button_label|default('Enregistrer') }}</button>
    <a href="{{ path('app_carrier_index') }}" class="btn btn-secondary">Retour à la liste</a>
{{ form_end(form) }}
