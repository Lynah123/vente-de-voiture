{% extends 'admin/base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
<section class="category-show">
    <div class="container">
        <h1>Category</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ category.id }}</td>
                </tr>
                <tr>
                    <th>Title</th>
                    <td>{{ category.title }}</td>
                </tr>
            </tbody>
        </table>
        <a href="{{ path('app_category_edit', {'id': category.id}) }}" class="btn btn-warning px-4 btn-register">Modifier</a>
    
            <a href="{{ path('app_category_index') }}" class="btn btn-secondary px-4">Retour à la liste</a>
        
        {{ include('admin/category/_delete_form.html.twig') }}
    </div>
</section>
    
{% endblock %}
