{% extends 'admin/base.html.twig' %}

{% block title %}Marque{% endblock %}

{% block body %}
<section class="brand-show">
    <div class="container">
        <h1>Marque</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Identification</th>
                    <td>{{ brand.id }}</td>
                </tr>
                <tr>
                    <th>Marque</th>
                    <td>{{ brand.title }}</td>
                </tr>
            </tbody>
        </table>
    
        <a href="{{ path('app_brand_edit', {'id': brand.id}) }}" class="btn btn-warning px-4 btn-register">Modifier</a>

        <a href="{{ path('app_brand_index') }}" class="btn btn-secondary px-4">Retour à la liste</a>
    
        {{ include('admin/brand/_delete_form.html.twig') }}
    </div>
</section>
   
{% endblock %}
