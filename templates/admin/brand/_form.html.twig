{{ form_start(form) }}
    <div class="form-group mb-3">
        <label for="" class="mb-2">Marque</label>
        <span class="text-danger"> {{form_errors(form.title)}} </span>
        {{form_widget(form.title)}}
    </div>
    <div class="form-group mb-3">
        <label for="" class="mb-2">Fournisseur</label>
        <span class="text-danger"> {{form_errors(form.supplier)}} </span>
        {{form_widget(form.supplier)}}
    </div>
    <button class="btn btn-primary btn-register px-4">{{ button_label|default('Enregistrer') }}</button>
    <a href="{{ path('app_brand_index') }}" class="btn btn-secondary px-4">Retour à la liste</a>

{{ form_end(form) }}
