<form class="mt-4" method="post" action="{{ path('app_type_delete', {'id': type.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">
    <button class="btn btn-danger px-4">Supprimer</button>
</form>
