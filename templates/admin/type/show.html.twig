{% extends 'admin/base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
<section class="type-show">
    <div class="container">
        <h1>Type</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Identification</th>
                    <td>{{ type.id }}</td>
                </tr>
                <tr>
                    <th>Titre</th>
                    <td>{{ type.title }}</td>
                </tr>
            </tbody>
        </table>
    
        <a href="{{ path('app_type_edit', {'id': type.id}) }}" class="btn btn-warning px-4 btn-register">Modifier</a>

        <a href="{{ path('app_type_index') }}" class="btn btn-secondary px-4">Retour à la liste</a>
    
        {{ include('admin/type/_delete_form.html.twig') }}
    </div>
</section>
    
{% endblock %}
